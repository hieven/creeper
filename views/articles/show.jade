extends ../layout

block content

  #article-show
    .panel.panel-default
      .panel-heading
        h3 #{article.title}
      .panel-body
        div.image-wrapper
          if article.image
            img.capture(src="#{article.image}", alt="capture")
          else
            img.capture(src="#{noPhoto}", alt="capture")

        .content
          !{article.content}
        - if (user)
          button#complete-btn.btn.btn-azure(data-article-id="#{article.id}",
                                            data-category="#{article.category.dataValues.name}") 
            | 閱讀完成
  #search-result.animated.hide
    button.close.pull-right(type="button", aria-label="Close")
      span(aria-hidden="true")
        | &times;
    p.result
    button#add-vocab.btn.btn-azure.pull-right 加入魚譜
append bottom
  script(src="/javascripts/articles/show.js")